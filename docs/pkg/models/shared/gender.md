# Gender

## Example Usage

```go
import (
	"github.com/unified-to/unified-go-sdk/pkg/models/shared"
)

value := shared.GenderMale

// Open enum: custom values can be created with a direct type cast
custom := shared.Gender("custom_value")
```


## Values

| Name           | Value          |
| -------------- | -------------- |
| `GenderMale`   | MALE           |
| `GenderFemale` | FEMALE         |