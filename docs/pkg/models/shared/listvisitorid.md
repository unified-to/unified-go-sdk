# ListVisitorID

## Example Usage

```go
import (
	"github.com/unified-to/unified-go-sdk/pkg/models/shared"
)

value := shared.ListVisitorIDSupportedRequired

// Open enum: custom values can be created with a direct type cast
custom := shared.ListVisitorID("custom_value")
```


## Values

| Name                             | Value                            |
| -------------------------------- | -------------------------------- |
| `ListVisitorIDSupportedRequired` | supported-required               |
| `ListVisitorIDSupported`         | supported                        |
| `ListVisitorIDNotSupported`      | not-supported                    |