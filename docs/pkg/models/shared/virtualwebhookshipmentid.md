# VirtualWebhookShipmentID

## Example Usage

```go
import (
	"github.com/unified-to/unified-go-sdk/pkg/models/shared"
)

value := shared.VirtualWebhookShipmentIDSupportedRequired

// Open enum: custom values can be created with a direct type cast
custom := shared.VirtualWebhookShipmentID("custom_value")
```


## Values

| Name                                        | Value                                       |
| ------------------------------------------- | ------------------------------------------- |
| `VirtualWebhookShipmentIDSupportedRequired` | supported-required                          |
| `VirtualWebhookShipmentIDSupported`         | supported                                   |
| `VirtualWebhookShipmentIDNotSupported`      | not-supported                               |