# ListSize

## Example Usage

```go
import (
	"github.com/unified-to/unified-go-sdk/pkg/models/shared"
)

value := shared.ListSizeSupportedRequired

// Open enum: custom values can be created with a direct type cast
custom := shared.ListSize("custom_value")
```


## Values

| Name                        | Value                       |
| --------------------------- | --------------------------- |
| `ListSizeSupportedRequired` | supported-required          |
| `ListSizeSupported`         | supported                   |
| `ListSizeNotSupported`      | not-supported               |